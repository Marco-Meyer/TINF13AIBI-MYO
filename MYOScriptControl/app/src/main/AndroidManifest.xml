<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.myo.myoscriptcontrol" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".GestureListActivity"
            android:label="@string/title_activity_gesture_list" >
        </activity>
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".GestureEditActivity"
            android:label="@string/title_activity_gesture_edit" >
        </activity>
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".ScriptListActivity"
            android:label="@string/title_activity_script_list" >
        </activity>
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".ScriptEditActivity"
            android:label="@string/title_activity_script_edit" >
        </activity>
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".FileExplorerActivity"
            android:label="@string/title_activity_file_explorer"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar" >
        </activity>
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".GestureRecordActivity"
            android:label="@string/title_activity_gesture_record" >
        </activity>
        <activity
            android:name=".ErrorActivity"
            android:label="@string/title_activity_error" >
        </activity>
    </application>

</manifest>
